{
  "name": "insighta-stage3-web",
  "version": "1.0.0",
  "private": true,
  "description": "Insighta Labs+ Stage 3 web portal",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "node --check lib/csrf.cjs && node --check next.config.js",
    "test": "node --test test/csrf.test.js"
  },
  "dependencies": {
    "next": "^15.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "engines": {
    "node": ">=20"
  }
}
